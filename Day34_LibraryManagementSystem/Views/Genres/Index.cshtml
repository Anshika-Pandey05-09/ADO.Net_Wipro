@model IEnumerable<Day34_LibraryManagementSystem.Models.Genre>
@{
    Layout = "_Layout";
}
<div class="d-flex justify-content-between align-items-center mb-3">
  <h2>Genres</h2>
  <button class="btn btn-primary" data-ajax="true" data-url="@Url.Action("Create")" data-title="Create Genre">Create</button>
</div>
<div id="listContainer">@Html.Partial("_List", Model)</div>