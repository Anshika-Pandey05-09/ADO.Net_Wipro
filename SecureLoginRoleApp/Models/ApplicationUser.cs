using Microsoft.AspNetCore.Identity;

namespace SecureLoginRoleApp.Models
{
    // Extend IdentityUser to store custom user info if needed
    public class ApplicationUser : IdentityUser
    {
    }
}